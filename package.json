{
    "name": "bistro",
    "private": true,
    "description": "App bistro (bistro). backend/ = API Laravel, frontend/ = SPA React. Scripts: `npm run backend`, `npm run queue`, `npm run frontend`, `npm run dev` (los tres).",
    "scripts": {
        "backend": "cd backend && php artisan serve --port=80",
        "queue": "cd backend && php artisan queue:listen --tries=1",
        "seed": "cd backend && php artisan migrate:fresh --seed --force",
        "frontend": "cd frontend && npm run dev",
        "dev": "npx --yes concurrently --kill-others --names \"API,QUEUE,WEB\" --prefix-colors \"blue,magenta,green\" \"npm run backend\" \"npm run queue\" \"npm run frontend\""
    }
}